// Image slots that aren't tied to a single course (content.ts only carries
// per-course `img` fields). Drop real files into public/assets/ per
// public/assets/README.md, then fill in the matching path below — every
// consumer (HeroMarquee, About) already falls back to a placeholder when a
// slot is empty, so this is the only file to touch.
export const heroMarqueeImages: string[] = ["", "", "", "", "", "", "", ""];

export const founderPortrait = "";
